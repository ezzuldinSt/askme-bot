/target
.env
.token.json
.processed-ids.json
output.log
.DS_Store

# Admin panel config (contains API keys + password hash)
bot_config.json
bot_config.json.bak

# Gaming mode state (per-instance data written by the bot)
game_states.json

# Support FAQ store (per-instance data written by the admin panel)
support_faqs.json

# Qdrant data
qdrant_data/

# opencode agent config (skills, commands)
.opencode/

# Local Gemini API reference docs (kept out of the repo on purpose)
Gemini-API-Reference/
